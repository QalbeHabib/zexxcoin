# Solana RPC URL (use devnet for testing)
SOLANA_RPC_URL=https://api.devnet.solana.com

# Your wallet private key (comma-separated numbers)
# To get this:
# 1. Save your keypair file
# 2. Run: cat your-keypair.json | jq -r '.privateKey | join(",")'
WALLET_PRIVATE_KEY= 